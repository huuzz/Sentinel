# SentinelAI ML

Run `cd backend && uv run python ../ml/training/train.py` to regenerate the dataset and model with
seed 42. The model uses synthetic educational data only and must not be presented as evidence of
real-world detection accuracy. The committed SHA-256 file protects the trusted bundled artifact from
accidental replacement; never load user-supplied joblib files.
